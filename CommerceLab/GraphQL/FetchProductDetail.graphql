query FetchProductDetail($productId: String) {
  product(id: $productId) {
    pictureModels {
      imageUrl
      thumbImageUrl
      fullSizeImageUrl
    }
    productType
    name
    shortDescription
    fullDescription
    productPrice {
      price
    }
    id
  }
}
